# tt 版本紀錄

## v3.0 · 九包版（機制學雷蒙 mini-course，內容照我們的）

- **總機＋九包**：`AGENTS.md` 只做找家、盤點、決定下一包、抓包；每一包（`kits/01`–`09`＋`upgrade`）10–90 分鐘做一件事，自帶環境偵測，做完就結束。狀態存在檔案裡，不存在腳本裡
- **所有問題選擇題**：Claude Code 用 AskUserQuestion、Codex 用編號純文字；≤ 4 選項、推薦第一、每個選項一句後果、最後永遠「其他」。六題訪談全部改成選項＋追問
- **每包固定骨架**：人看的前半（問題／得到什麼／怎麼啟動）→ AI 執行區 Section 0（平台、系統、找家、前提）→ A 訪談 → B 動手（Mac／Windows 兩組＋備援）→ C 寫檔（`<!-- tt:kit-NN START/END -->` 疊加不覆蓋，重跑只換標記裡面）→ D 秀給他看＋下次三步＋明天的作業 → E 可執行的完成清單 → 踩坑紀錄／FAQ
- **啟動詞帶防呆**（完整讀、一次一題給選項、先找現成的 xxx-agent 不叫我換資料夾）
- `onboarding.md` 降級成給學員看的九包進度表；舊七段／三站格式自動改寫
- `stations/*.md` 只留指標
- 為什麼這樣改：2026-09-13 真人測試三個問題（沒找到旁邊的家／「然後咧」／自檢考路徑）都是「開放式互動＋跨天狀態機」的產物；拆包＋選項題是根治

## v2.2 · 第一次真人測試後的修正（Tim 用 Codex 測，2026-09-13）

- **先掃環境再開口**：入口檔新增第零步——看全域入口指到哪、看目前資料夾、全機搜 `*-agent`；找到就用絕對路徑在那裡工作。測試時 tt 只看眼前的空資料夾，沒發現 `julia-agent` 就在旁邊
- **整趟不叫他換資料夾、不叫他去別的對話驗證**：第 0 段建家改成「建好就在那裡工作，下次開對話選它」；第 1 段的「重開看變化」與第 2 段的「到別的資料夾測全域」改成 tt 自己讀回檔案驗證，真人示範變成站尾選做。測試時學員被要求去別的對話驗證，回來只能問「然後咧」
- **自檢改成「秀給他看」**：三站的三題自檢不再考他，tt 把路徑、他寫的原文、入口內容擺出來請他看一眼；答錯不說「差一點」。測試時 tt 要學員背出完整路徑
- 新增執行規則 6–9：先掃環境、等他動手要講四件事（目標／步驟／回來說什麼／做不到怎麼辦）、自檢不是考試、不提這段用不到的工具（git 之類）
- 0-1 說清楚「我叫你 X；分身的名字第 1 段再取」，避免把人跟分身混成同一個名字

## v2.1 · 小白視角修訂

- 審稿修正 40 餘處：全域驗證改到別的資料夾測、2-3 補備援、4-6 明寫「接在既有小節後面」、健檢六項順序統一、Windows 指令（irm／Get-ChildItem／Get-PSDrive／uv 與驗證各給兩組）、Codex 命令列的換資料夾說法、流程檔名改英文、成品放 projects/practice/、刪腳本前先改規矩、第 3 段時間改 90 分、非學員跳過傳老師、已完成段落一律跳過
- **每次對話開始都盤點資料夾**（只讀），以檔案為準修正進度表；沒有進度表但資料夾有東西 → 抓 `stations/upgrade.md`：盤點 → 體檢報告 → 升級計畫 → 點頭才動 → 建進度表補勾 → 接下去。三條原則：不刪、先備份、先列計畫
- **換資料夾移到最前面**：站錯地方時，tt 先替他建好資料夾、請他換過去再貼同一段話，這時什麼都還沒建；0-1 加短版開口（看到 `-agent` 資料夾直接住進去、從名字確認稱呼）。原本的 0-5 交接取消，第 0 段做完直接接訪談
- **Obsidian 移到第三站**：第二站只建 schema，Obsidian 記進待辦回家裝；第三站 3-7 帶他裝好、開 vault、再接 MCP
- **每一段開場多一句「現在要做什麼、大概幾分鐘、你要不要動手」**
- **每站結尾三題自檢**，寫進 `onboarding.md` 新增的「我做得到什麼」區
- 第 4 段待辦那題給例子；第 7 段 Windows 備援排順序（winget 鎖 → irm → python.org → 跳過）
- 第 8 段測「收工」時不再問要不要傳（早上傳過了）
- 子資料夾規矩改成 `AGENTS.md` 本尊＋一行 `CLAUDE.md`（Codex 學員原本讀不到 `knowledge/` 的 schema）

## v2.0 · 三站版

- 一份腳本拆成入口＋三站：`AGENTS.md`（入口與判斷）、`stations/station-1.md`（段 0–2）、`stations/station-2.md`（段 4、7、5、6、8、9）、`stations/station-3.md`（段 3、10、11）。tt 需要哪一站才抓哪一站——規則檔只留索引，內容放外面
- 段落編號固定、站是播放順序。舊版七段的 `onboarding.md` 會被自動升級成三站格式，做過的都保留
- 進第二站前「以檔案為準」：在家自己往下跑過的人，tt 看資料夾裡實際有什麼來補勾
- 新段落：**7 給它工具**（十個核心套件裝進 `tools/.venv`、整理資料夾、出第一份成品、yt-dlp 選裝）、**8 第一招 daily-log**（觸發寫在規矩兩行、程序寫在招式）、**9 用講的**（語音輸入＋文字正規化規矩）
- 第 4 段加 4-8：第一篇日誌傳給老師（走平台的 share.md）
- 第 6 段的第一份原料改成「第一堂回顧」，讀完就是一個懂這門課的 Agent；健檢規矩改寫進 `core-rules.md`（v1.0 誤寫成 `CLAUDE.md`）
- Obsidian MCP 從第 5 段移到第 3 段（第三站），第 3 段補「鑰匙三種、通道四種」
- 第 10、11 段先放骨架，第三堂前補完

## v1.0

- 七段單檔版：建家、認識你、兩層規則檔、MCP、記憶、第二大腦、第一次自主學習
